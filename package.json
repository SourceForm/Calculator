{
  "name": "calculatorapp",
  "version": "1.0.0",
  "description": "Built application for calculator web page",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ."
  },
  "private": true,
  "dependencies": {
    "electron": "^29.3.0"
  }
}
